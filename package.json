{
  "name": "cooked-finance-home-cost",
  "type": "module",
  "version": "1.0.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "verify:home": "node scripts/run-home-cost-verify.mjs",
    "verify:score": "node scripts/run-score-verify.mjs",
    "verify:site": "node scripts/site-verify.mjs",
    "verify": "astro build && node scripts/run-home-cost-verify.mjs && node scripts/run-score-verify.mjs && node scripts/site-verify.mjs",
    "preview": "wrangler dev",
    "astro": "astro",
    "deploy": "npm run verify && wrangler deploy"
  },
  "dependencies": {
    "@fontsource-variable/geist": "^5.2.9",
    "@fontsource-variable/geist-mono": "^5.2.8",
    "@tailwindcss/vite": "^4.3.0",
    "astro": "^6.4.4",
    "tailwindcss": "^4.3.0"
  },
  "devDependencies": {
    "esbuild": "^0.27.7",
    "wrangler": "^4.98.0"
  }
}

<div class="hero-copy">
  <span class="hero-copy__kicker">
    <span aria-hidden="true">●</span> Private financial heat check
  </span>

  <h1>A Financial Health Check That Shows Your <em>Next Move.</em></h1>

  <p class="hero-copy__lead">
    Seven reasonable estimates become one clear score, four transparent signals, and a practical action plan—without connecting a bank account.
  </p>

  <div class="hero-copy__actions">
    <a href="#check" class="btn-primary" data-scroll>
      Check My Financial Health
      <svg width="17" height="17" viewBox="0 0 17 17" fill="none" aria-hidden="true">
        <path d="M3 8.5h11M10 4.5l4 4-4 4" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" />
      </svg>
    </a>
    <a href="/methodology/" class="hero-copy__method">Read the methodology</a>
  </div>

  <ul class="hero-copy__trust" aria-label="Key facts">
    <li><span aria-hidden="true">✓</span> Runs on your device</li>
    <li><span aria-hidden="true">✓</span> No signup</li>
    <li><span aria-hidden="true">✓</span> Transparent formulas</li>
  </ul>

  <div class="hero-copy__note">
    <span class="hero-copy__note-mark" aria-hidden="true">01</span>
    <p><strong>Serious math, light delivery.</strong> The score is an educational estimate—not a credit score, approval signal, or financial advice.</p>
  </div>
</div>

<style>
  .hero-copy { align-self: center; padding-block: clamp(1rem, 4vw, 3rem); }
  .hero-copy__kicker { display: inline-flex; align-items: center; gap: 0.55rem; color: var(--color-sage); font-family: var(--font-mono); font-size: 0.72rem; font-weight: 680; letter-spacing: 0.07em; text-transform: uppercase; }
  .hero-copy__kicker span { color: var(--color-ember); font-size: 0.58rem; }
  h1 { max-width: 12ch; margin: 1rem 0 0; font-family: var(--font-serif); font-size: clamp(3.35rem, 7.4vw, 6.7rem); font-weight: 500; letter-spacing: -0.055em; line-height: 0.91; }
  h1 em { color: var(--color-ember); font-weight: inherit; }
  .hero-copy__lead { max-width: 54ch; margin: 1.5rem 0 0; color: var(--color-body); font-size: clamp(1.05rem, 2vw, 1.22rem); line-height: 1.65; }
  .hero-copy__actions { margin-top: 1.8rem; display: flex; flex-wrap: wrap; align-items: center; gap: 1rem 1.2rem; }
  .hero-copy__method { color: var(--color-body); font-size: 0.86rem; font-weight: 650; text-underline-offset: 0.22em; }
  .hero-copy__method:hover { color: var(--color-ink); }
  .hero-copy__trust { margin: 1.7rem 0 0; padding: 0; display: flex; flex-wrap: wrap; gap: 0.65rem 1.25rem; color: var(--color-body); font-size: 0.8rem; list-style: none; }
  .hero-copy__trust li { display: inline-flex; align-items: center; gap: 0.38rem; }
  .hero-copy__trust span { color: var(--color-sage); font-weight: 800; }
  .hero-copy__note { max-width: 36rem; margin-top: clamp(2.3rem, 6vw, 4.5rem); padding-top: 1rem; display: grid; grid-template-columns: auto 1fr; gap: 0.9rem; border-top: 1px solid var(--color-hairline-strong); }
  .hero-copy__note-mark { width: 2.1rem; height: 2.1rem; display: grid; place-items: center; border-radius: 50%; background: var(--color-ink); color: var(--color-paper); font-family: var(--font-mono); font-size: 0.66rem; }
  .hero-copy__note p { margin: 0; color: var(--color-body); font-size: 0.83rem; line-height: 1.55; }
  .hero-copy__note strong { color: var(--color-ink); }
  @media (max-width: 520px) {
    h1 { font-size: clamp(3rem, 15vw, 4.4rem); }
    .hero-copy__actions { align-items: stretch; flex-direction: column; }
    .hero-copy__method { text-align: center; }
  }
</style>

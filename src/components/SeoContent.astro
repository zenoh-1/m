<section id="how-it-works" class="section measure">
  <div class="container-page">
    <div class="section-head">
      <span class="eyebrow">Four Signals, One Estimate</span>
      <h2 class="section-title">A Score You Can Actually Inspect</h2>
      <p class="section-lead">Every point comes from a visible factor. No credit pull, secret peer ranking, or invented percentile.</p>
    </div>

    <div class="measure__grid">
      <article class="measure-card measure-card--buffer">
        <div class="measure-card__top"><span>01</span><strong>30%</strong></div>
        <h3>Cash Buffer</h3>
        <p>How many months of essential expenses your accessible savings could cover.</p>
        <div class="mini-scale" aria-hidden="true"><span style="width:72%"></span></div>
      </article>
      <article class="measure-card measure-card--debt">
        <div class="measure-card__top"><span>02</span><strong>25%</strong></div>
        <h3>Debt Burden</h3>
        <p>Required monthly debt payments compared with gross monthly income.</p>
        <div class="mini-scale" aria-hidden="true"><span style="width:54%"></span></div>
      </article>
      <article class="measure-card measure-card--habit">
        <div class="measure-card__top"><span>03</span><strong>20%</strong></div>
        <h3>Savings Habit</h3>
        <p>The share of gross monthly income currently moving into savings or investments.</p>
        <div class="mini-scale" aria-hidden="true"><span style="width:61%"></span></div>
      </article>
      <article class="measure-card measure-card--future">
        <div class="measure-card__top"><span>04</span><strong>25%</strong></div>
        <h3>Long-Term Progress</h3>
        <p>Retirement and investment assets compared with an age-based savings guidepost.</p>
        <div class="mini-scale" aria-hidden="true"><span style="width:45%"></span></div>
      </article>
    </div>

    <div class="method-strip">
      <div>
        <span class="method-strip__number">v2.0</span>
        <p><strong>Open methodology.</strong> Formulas, weights, curves, assumptions, sources, and revisions live on one readable page.</p>
      </div>
      <a href="/money-tools/financial-health-check/methodology/" class="btn-secondary">Inspect the Method</a>
    </div>
  </div>
</section>

<section class="section section--paper explore">
  <div class="container-page">
    <div class="section-head section-head--center">
      <span class="eyebrow">Go One Layer Deeper</span>
      <h2 class="section-title">Useful Tools, Not Content Confetti</h2>
      <p class="section-lead">Each calculator answers one specific question and shows the formula behind the answer.</p>
    </div>

    <div class="content-cards">
      <a href="/calculators/debt-to-income/" class="content-card card">
        <span class="tag">Debt</span>
        <h3 class="content-card__title">Debt-to-Income Calculator</h3>
        <p class="content-card__copy">Calculate payment-based DTI correctly and understand what the percentage can—and cannot—tell you.</p>
        <span class="content-card__link">Calculate DTI →</span>
      </a>
      <a href="/calculators/emergency-fund/" class="content-card card">
        <span class="tag">Resilience</span>
        <h3 class="content-card__title">Emergency Fund Calculator</h3>
        <p class="content-card__copy">Translate accessible savings into months of essential runway and a practical target gap.</p>
        <span class="content-card__link">Check my runway →</span>
      </a>
      <a href="/calculators/net-worth/" class="content-card card">
        <span class="tag">Balance Sheet</span>
        <h3 class="content-card__title">Complete Net Worth Calculator</h3>
        <p class="content-card__copy">Count your full set of assets and liabilities instead of calling cash minus debt “net worth.”</p>
        <span class="content-card__link">Calculate net worth →</span>
      </a>
    </div>

    <div class="guides-row">
      <div class="guides-row__intro">
        <span class="eyebrow">Action Guides</span>
        <h2>Read Less. Leave With a Plan.</h2>
      </div>
      <nav aria-label="Featured financial health guides">
        <a href="/guides/how-to-improve-financial-health/"><span>01</span>How to improve financial health <b aria-hidden="true">↗</b></a>
        <a href="/guides/what-to-do-after-a-low-score/"><span>02</span>What to do after a low score <b aria-hidden="true">↗</b></a>
        <a href="/guides/quarterly-money-checkup/"><span>03</span>Run a quarterly money checkup <b aria-hidden="true">↗</b></a>
      </nav>
    </div>
  </div>
</section>

<style>
  .measure__grid { display: grid; grid-template-columns: repeat(4, minmax(0, 1fr)); border: 1px solid var(--color-hairline); border-radius: var(--radius-lg); overflow: hidden; background: var(--color-paper); }
  .measure-card { min-width: 0; min-height: 19rem; padding: 1.3rem; display: flex; flex-direction: column; border-right: 1px solid var(--color-hairline); }
  .measure-card:last-child { border-right: 0; }
  .measure-card__top { display: flex; align-items: center; justify-content: space-between; color: var(--color-mute); font-family: var(--font-mono); font-size: 0.68rem; }
  .measure-card__top strong { color: var(--color-ink); font-size: 0.82rem; }
  .measure-card h3 { margin: auto 0 0.7rem; font-family: var(--font-serif); font-size: clamp(1.45rem, 3vw, 2rem); font-weight: 500; line-height: 1; }
  .measure-card p { min-height: 5.2rem; margin: 0; color: var(--color-body); font-size: 0.85rem; line-height: 1.55; }
  .mini-scale { height: 5px; margin-top: 1.1rem; overflow: hidden; border-radius: 99px; background: var(--color-canvas-soft); }
  .mini-scale span { display: block; height: 100%; border-radius: inherit; background: var(--color-sage); }
  .measure-card--debt .mini-scale span { background: var(--color-ember); }
  .measure-card--habit .mini-scale span { background: var(--color-amber); }
  .measure-card--future .mini-scale span { background: #806957; }
  .method-strip { margin-top: 1rem; padding: 1.15rem 1.3rem; display: flex; align-items: center; justify-content: space-between; gap: 1rem; border: 1px solid var(--color-hairline); border-radius: var(--radius-lg); background: rgba(255,252,247,0.65); }
  .method-strip > div { display: grid; grid-template-columns: auto 1fr; align-items: center; gap: 1rem; }
  .method-strip__number { width: 3.1rem; height: 3.1rem; display: grid; place-items: center; border-radius: 50%; background: var(--color-ink); color: var(--color-paper); font-family: var(--font-mono); font-size: 0.7rem; }
  .method-strip p { max-width: 50ch; margin: 0; color: var(--color-body); font-size: 0.84rem; line-height: 1.5; }
  .method-strip strong { color: var(--color-ink); }
  .explore .content-card { transition: transform 160ms var(--ease-out-expo), border-color 160ms ease; }
  .guides-row { margin-top: clamp(4rem, 9vw, 7.5rem); display: grid; grid-template-columns: 0.8fr 1.2fr; gap: clamp(2rem, 8vw, 7rem); align-items: start; }
  .guides-row__intro h2 { max-width: 10ch; margin: 0.65rem 0 0; font-family: var(--font-serif); font-size: clamp(2.2rem, 5vw, 3.8rem); font-weight: 500; letter-spacing: -0.04em; line-height: 0.98; }
  .guides-row nav { display: grid; border-top: 1px solid var(--color-hairline-strong); }
  .guides-row nav a { min-height: 74px; display: grid; grid-template-columns: auto 1fr auto; align-items: center; gap: 1rem; border-bottom: 1px solid var(--color-hairline); color: var(--color-ink); font-size: 0.96rem; font-weight: 650; text-decoration: none; }
  .guides-row nav a:hover { color: var(--color-ember-deep); }
  .guides-row nav span { color: var(--color-mute); font-family: var(--font-mono); font-size: 0.68rem; }
  .guides-row nav b { font-size: 1.1rem; }
  @media (max-width: 920px) {
    .measure__grid { grid-template-columns: 1fr 1fr; }
    .measure-card:nth-child(2) { border-right: 0; }
    .measure-card:nth-child(-n+2) { border-bottom: 1px solid var(--color-hairline); }
  }
  @media (max-width: 720px) {
    .method-strip, .guides-row { align-items: stretch; grid-template-columns: 1fr; flex-direction: column; }
    .method-strip { display: flex; }
    .guides-row__intro h2 { max-width: 14ch; }
  }
  @media (max-width: 520px) {
    .measure__grid { grid-template-columns: 1fr; }
    .measure-card { min-height: 14rem; border-right: 0; border-bottom: 1px solid var(--color-hairline); }
    .measure-card:last-child { border-bottom: 0; }
    .measure-card p { min-height: auto; }
    .measure-card h3 { margin-top: 3rem; }
    .method-strip > div { grid-template-columns: 1fr; }
  }
</style>

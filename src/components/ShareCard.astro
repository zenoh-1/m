<div class="sharewrap">
  <div class="sharewrap__head">
    <div>
      <span class="eyebrow">Privacy-Safe Sharing</span>
      <h3>Share the heat, not your finances.</h3>
    </div>
    <p>The card includes only your score, score band, and top signal. No income, debt, savings, or result data is added to the link.</p>
  </div>

  <div class="sharecard" data-share-card>
    <div class="sharecard__brand"><span aria-hidden="true">●</span> COOKED FINANCE</div>
    <div class="sharecard__middle">
      <div><span data-share-score>0</span><small>/100</small></div>
      <p data-share-badge>Finding Balance</p>
    </div>
    <p class="sharecard__strength"><span>MY TOP SIGNAL</span><strong data-share-strength>Ready when you are</strong></p>
    <p class="sharecard__cta">What temperature are your finances? · cookedfinance.com</p>
  </div>

  <canvas data-share-canvas width="1200" height="630" hidden></canvas>

  <div class="sharewrap__actions">
    <button type="button" class="btn-primary" data-download-share>
      <svg width="16" height="16" viewBox="0 0 16 16" fill="none" aria-hidden="true"><path d="M8 2v8m0 0L5 7m3 3 3-3M3 13h10" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" /></svg>
      Download Share Card
    </button>
    <button type="button" class="btn-secondary" data-share-native>
      <svg width="16" height="16" viewBox="0 0 16 16" fill="none" aria-hidden="true"><path d="M11 5a2 2 0 1 0-1.9-1.4L6.1 5.3a2 2 0 1 0 0 5.4l3 1.7a2 2 0 1 0 .6-1L6.8 9.7a2 2 0 0 0 0-3.4l2.9-1.7c.36.25.8.4 1.3.4Z" fill="currentColor" /></svg>
      Share Challenge
    </button>
    <button type="button" class="btn-quiet" data-copy-link>Copy Link</button>
  </div>
  <p class="sharewrap__toast" data-share-toast role="status" aria-live="polite"></p>
</div>

<style>
  .sharewrap { display: grid; grid-template-columns: minmax(0, 0.75fr) minmax(18rem, 1.25fr); gap: clamp(1.5rem, 5vw, 4rem); align-items: center; }
  .sharewrap__head h3 { margin: 0.65rem 0 0; font-family: var(--font-serif); font-size: clamp(2rem, 4vw, 3rem); font-weight: 500; letter-spacing: -0.04em; line-height: 1; }
  .sharewrap__head > p { max-width: 40ch; margin: 1rem 0 0; color: var(--color-body); font-size: 0.86rem; line-height: 1.6; }
  .sharecard { min-height: 310px; padding: clamp(1.25rem, 4vw, 2rem); display: flex; flex-direction: column; border-radius: var(--radius-xl); background: var(--color-ink); color: var(--color-paper); box-shadow: var(--shadow-raised); }
  .sharecard__brand { display: flex; align-items: center; gap: 0.45rem; color: rgba(255,252,247,0.72); font-family: var(--font-mono); font-size: 0.68rem; letter-spacing: 0.08em; }
  .sharecard__brand span { color: var(--color-amber); }
  .sharecard__middle { margin-top: auto; display: flex; align-items: end; justify-content: space-between; gap: 1rem; }
  .sharecard__middle > div { display: flex; align-items: baseline; }
  .sharecard__middle [data-share-score] { font-family: var(--font-serif); font-size: clamp(4.4rem, 12vw, 7rem); line-height: 0.75; letter-spacing: -0.07em; font-variant-numeric: tabular-nums; }
  .sharecard__middle small { color: rgba(255,252,247,0.55); font-size: 1rem; }
  .sharecard__middle > p { max-width: 12ch; margin: 0; color: var(--color-amber); font-size: 0.85rem; font-weight: 720; text-align: right; text-transform: uppercase; }
  .sharecard__strength { margin: 1.5rem 0 0; padding-top: 0.8rem; display: flex; align-items: baseline; justify-content: space-between; gap: 1rem; border-top: 1px solid rgba(255,252,247,0.17); }
  .sharecard__strength span { color: rgba(255,252,247,0.72); font-family: var(--font-mono); font-size: 0.7rem; letter-spacing: 0.07em; }
  .sharecard__strength strong { font-size: 0.8rem; text-align: right; }
  .sharecard__cta { margin: 0.8rem 0 0; color: rgba(255,252,247,0.72); font-size: 0.75rem; }
  .sharewrap__actions { grid-column: 2; display: flex; flex-wrap: wrap; justify-content: center; gap: 0.65rem; }
  .sharewrap__toast { grid-column: 2; min-height: 1rem; margin: -0.6rem 0 0; color: var(--color-sage); font-size: 0.75rem; text-align: center; }
  @media (max-width: 800px) {
    .sharewrap { grid-template-columns: 1fr; }
    .sharewrap__actions, .sharewrap__toast { grid-column: auto; }
  }
  @media (max-width: 520px) {
    .sharecard__middle, .sharecard__strength { align-items: flex-start; flex-direction: column; }
    .sharecard__middle > p, .sharecard__strength strong { max-width: none; text-align: left; }
  }
</style>
